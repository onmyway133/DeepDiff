import Foundation

/// Perform diff between old and new collections
///
/// - Parameters:
///   - old: old collection
///   - new: new collection
/// - Returns: A set of changes
public func diff<T>(old: Array<T>, new: Array<T>) -> [Change<T>] {
  return []
}
